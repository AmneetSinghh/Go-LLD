package main

/*


✅ Use Mediator Pattern When:

You need to reduce dependencies between multiple objects.
Objects should not communicate directly but instead through a central entity.
You want to make each component reusable and independent.


its kind of a manager. which manages multiple dependencies. its a central system which multiple objects interact.const


object1    - mediater
object2    - mediater
object3    - mediater
*/
